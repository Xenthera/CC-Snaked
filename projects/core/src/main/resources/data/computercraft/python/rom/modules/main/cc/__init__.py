"""ComputerCraft ``cc`` package (Python).

Mirrors the Lua ROM layout at ``rom/modules/main/cc/*``. Submodules expose
the user-facing APIs (``cc.os``, ``cc.term``, ``cc.fs``, ``cc.shell`` ...)
that programs ported from the Lua ROM call into.
"""
